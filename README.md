# TicTacTeo
This HTML code creates a simple **Tic Tac Toe game** with a title, 3x3 grid using buttons, and controls like "New Game" and "Reset". The winner message is displayed when the game ends. Styling is handled by `style.css`, and game logic is added via `index.js` JavaScript file.
